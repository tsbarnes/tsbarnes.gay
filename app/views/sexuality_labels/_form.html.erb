<%= form_with(model: sexuality_label) do |form| %>
  <% if sexuality_label.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sexuality_label.errors.count, "error") %> prohibited this sexuality_label from being saved:</h2>

      <ul>
        <% sexuality_label.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
