<%= form_with(model: gender_label) do |form| %>
  <% if gender_label.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gender_label.errors.count, "error") %> prohibited this gender_label from being saved:</h2>

      <ul>
        <% gender_label.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
